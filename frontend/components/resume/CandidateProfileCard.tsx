import { PlaceholderPanel } from "@/components/ui/PlaceholderPanel";

export function CandidateProfileCard() {
  return (
    <PlaceholderPanel
      title="Structured profile"
      description="Education, skills, experience, projects, target roles, strengths, and verified source facts will appear here."
    />
  );
}
