import { PlaceholderPanel } from "@/components/ui/PlaceholderPanel";

export function MatchSummary() {
  return (
    <PlaceholderPanel
      title="Explainable match score"
      description="Component scores, matched evidence, missing requirements, and recommended actions will appear here."
    />
  );
}
