import { PlaceholderPanel } from "@/components/ui/PlaceholderPanel";

export function SuggestionReview() {
  return (
    <PlaceholderPanel
      title="Human review queue"
      description="Users will accept, edit, reject, or regenerate fact-linked suggestions before a resume version is created."
    />
  );
}
